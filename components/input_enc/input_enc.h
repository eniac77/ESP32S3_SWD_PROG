#pragma once
/* Gombos enkóder (ISR/PCNT) + nyomógomb (short/long) -> eseménysor (queue) */
#include "esp_err.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Komponens inicializálás (skeleton stub). */
esp_err_t input_enc_init(void);

#ifdef __cplusplus
}
#endif
